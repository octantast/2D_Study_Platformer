using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Size : MonoBehaviour
{
    private void Update()
    {
        //if(transform.localScale.x < 1)
        //{
        //    transform.localScale = Vector3.Lerp(transform.localScale, new Vector3(0, 0, 0f), 5f);
        //}
        //else
        //{
        // Time.timeScale = -0.1f;
        //}
        //Time.timeScale = Mathf.Lerp(Time.timeScale, 0, 2f * Time.deltaTime);
    }
}
